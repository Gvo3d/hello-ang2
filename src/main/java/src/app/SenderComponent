import{OnInit}from'@angular/core';

export abstract class AbstractComponent{
    sendData(protected ): void{
        let data = getId();
        console.log('Sending data to some method:'+data);
    }
}
